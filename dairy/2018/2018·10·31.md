# 2018-10-31

## 河南业务变更

>太极王国刚 2018/10/31 10:25:04
>
>注销时间、最后一次审核时间、初次发证时间、数据最后一次变化的类型这几个

## excel 技巧

- 表头不动
  - 视图-冻结窗格
- 下拉框体
  - 数据-有效性-序列-选择一存在的序列
- 未选定区域为空白
  - 视图-网格线
- 未选定区域为置灰
  - 选中不需要的首列，Ctrl+Shift+→，右键－隐藏。
  - 选中不需要的首行，Ctrl+Shift+↓，右键－隐藏。

## 甘肃专职委员同步

-- 更新完毕

```sql
insert into t_sys_manager
  (UUID,
   AREA_CODE,
   AREA_LEVEL,
   area_type,
   area_persons,
   name,
   citizen_id,
   gender,
   nation,
   birthday,
   education,
   policital_status,
   disabled_type,
   disabled_level,
   rel_to_disabled,
   towns_presid_member,
   welfare_post,
   subsidy_type,
   subsidy_money,
   address,
   tel,
   concurrent_post,
   work_time,
   work_type,
   train_status,
   train_days,
   work_evaluation,
   validation_results,
   assign_status,
   produce_time,
   edit_time,
   creator,
   editer,
   del_flag,
   id_card,
   certificate,
   yl_field1,
   input_time,
   trans_status,
   proceince_id)
  (select UUID,
          AREA_CODE,
          AREA_LEVEL,
          area_type,
          area_persons,
          name,
          id_card,
          gender,
          nation,
          birthday,
          education,
          policital_status,
          disabled_type,
          disabled_level,
          rel_to_disabled,
          towns_presid_member,
          welfare_post,
          subsidy_type,
          subsidy_money,
          address,
          tel,
          concurrent_post,
          work_time,
          work_type,
          train_status,
          train_days,
          work_evaluation,
          validation_results,
          assign_status,
          CREATE_TIME,
          edit_time,
          creator,
          editer,
          del_flag,
          CARDNUM,
          certificate,
          '1',
          to_char(sysdate, 'yyyy-mm-dd hh24:mi:ss'),
          '2',
          '62'
     from ZXDC_MANAGER@SERVICEINFO2018
    where del_flag = 2);
```

```sql
delete t_sys_manager t
 where and t.id_card in
       (select t.citizen_id
          from t_sys_manager_resp t
         where t.res_msg =
               '上岗时间错误,上岗时间WORK_TIME不能为空,且必须使用YYYY-MM-DD格式填写!'));
```